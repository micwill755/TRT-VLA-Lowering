GR00T
=====

**Aliases:** ``gr00t``, ``groot``

NVIDIA GR00T N1.x vision-language-action stack: preprocess, vision, language, action
context, and action stages exported as Edge-LLM–compatible TensorRT engines.

.. code-block:: bash

   python app.py --model gr00t
